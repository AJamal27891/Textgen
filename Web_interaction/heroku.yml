build:
  docker:
    web: Dockerfile
    worker: Dockerfile
run:
  web:
    command:
      - gunicorn API2:app
    image: web
  worker:
    command:
      - celery worker --app=main.celery
    image: worker
